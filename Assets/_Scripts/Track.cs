using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Track : MonoBehaviour
{
	public GameObject defaultCameraPosition;//where the camera should be during gameplay
	public GameObject billboardCameraPosition;//where the camera should be during post-race stats
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
